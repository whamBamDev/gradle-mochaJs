

apply plugin: 'groovy'
apply plugin: 'eclipse'

repositories {
    mavenLocal()
    mavenCentral()
    jcenter()
}

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile(
      [ "com.moowork.gradle:gradle-node-plugin:0.9" ]
  )
}

apply plugin: 'maven'

dependencies {
    testCompile 'junit:junit:4.+'
}

group = 'me.ineson.gradle'
version = '0.2-SNAPSHOT'


//task myScript(type: NodeTask) {
  // Need to add node execs to the classpath.
  // $ ./../DemoApp/node_modules/.bin/mocha  ../DemoApp/src/intTest/javascript/myTest.js

//  script = file('src/intTest/javascript/myTest.js')
//}

